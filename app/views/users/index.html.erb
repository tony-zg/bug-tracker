<h2>All Users</h2>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %>

      <% if @current_user.admin == true %>
        <%= link_to 'edit', edit_user_path(user.id) %>
      <% end  %>

    </li>
  <% end  %>
</ul>
